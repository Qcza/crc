import * as React from 'react';
import { Container } from './styles';

export interface Props {}

const **name**: React.FC<Props> = React.memo(() => {
  return <Container>**name**</Container>;
});

**name**.displayName = '**name**';

export default **name**;
